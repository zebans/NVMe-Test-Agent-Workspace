# KV Delete Payload Reference

Delete has no command data payload and no command-specific returned payload.

On successful completion (`SC=00h`), the specified KV key and associated KV value have been deleted.
