# ZNS-Modified Flush Selector Reference

| Condition | Meaning | Expected status direction |
|---|---|---|
| Specified zone is Offline | Zone involved in the Flush condition is `ZSO:Offline`. | `Zone Is Offline` (`BBh`). |

Base Flush selectors and fields remain owned by the NVM Command Set.
