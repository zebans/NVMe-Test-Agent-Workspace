# ZNS-Modified Write Cross-Spec Boundary

NVM owns the Write command fields and base semantics.

ZNS owns:

- zone-type requirements;
- write pointer restriction;
- ZNS command-specific status values.

API usage and test-flow design are outside this file.
