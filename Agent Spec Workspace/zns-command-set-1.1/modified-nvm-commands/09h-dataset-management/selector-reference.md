# ZNS-Modified Dataset Management Selector Reference

| Condition | Meaning | Expected status direction |
|---|---|---|
| Specified zone is Offline | A zone specified by the Dataset Management operation is `ZSO:Offline`. | `Zone Is Offline` (`BBh`). |

Base Dataset Management selectors, range attributes, and Source Range Entry-style structures remain owned by the NVM Command Set.
